const colors = {
  background: '#0f172a',
  text: '#ffffff',
  primary: '#1e40af',
  secondary: '#374151',
}

export const backgroundStyle = `bg-${colors.background}`
export const textStyle = `text-${colors.text}`
export const primaryStyle = `bg-${colors.primary}`
export const secondaryStyle = `bg-${colors.secondary}`

export default colors